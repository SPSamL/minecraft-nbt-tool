/// <summary>
/// Represents one unique block with its minimum observed level.
/// </summary>
internal sealed record UniqueBlockMinimumLevelRow(string Name, string Mod, int MinimumLevel, bool HasLevel);
